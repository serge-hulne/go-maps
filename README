Generalized maps for the Go Programming Language: 

These containers are targeted at storing aggregate types.
These maps accept as keys all interfaces which implement an Int() or a String() method.
This is an attempt at generalizing the built-in map type which does not deal with aggregate types.

Serge Hulne.




